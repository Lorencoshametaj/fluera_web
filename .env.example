# Fill these in from Supabase → Project Settings → API
# Both values are public by design (the anon key only grants access to
# the `submit_waitlist` RPC, which validates + inserts server-side).
PUBLIC_SUPABASE_URL=https://YOUR-PROJECT-REF.supabase.co
PUBLIC_SUPABASE_ANON_KEY=YOUR_ANON_PUBLIC_KEY

# ─── Analytics (Plausible) ─────────────────────────────────────────────
# If set, the Plausible script auto-loads in <Head>. Leave blank to skip.
# Self-hosting? Override PUBLIC_PLAUSIBLE_SRC to your own script URL.
PUBLIC_PLAUSIBLE_DOMAIN=
PUBLIC_PLAUSIBLE_SRC=https://plausible.io/js/script.js
